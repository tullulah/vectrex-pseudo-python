# Demo: rampa de intensidad para verificar que el bucle principal avanza.
# No definimos asignaciones globales fuera de funciones (el parser falla),
# así que creamos/actualizamos las "globales" tocándolas dentro de main.

def main():
    return 0
